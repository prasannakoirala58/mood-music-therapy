// TODO Phase 5 — custom hook for chat state and API calls
// Will handle: message history, loading state, POST to /api/recommend

export function useChat() {
  // placeholder
  return {
    messages: [],
    isLoading: false,
    sendMessage: async () => {},
  }
}
