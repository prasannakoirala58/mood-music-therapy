# Copy this file to .env and fill in your values
# NEVER commit .env — it is git-ignored

# OpenAI — used for mood text classification
OPENAI_API_KEY=

# Spotify — stored for future use, not used in Phases 0-4
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=